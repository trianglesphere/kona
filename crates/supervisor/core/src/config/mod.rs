//! Configuration management for the supervisor.

mod rollup_config_set;
pub use rollup_config_set::{Genesis, RollupConfig, RollupConfigSet};

// todo: think about merging all the core config into one
